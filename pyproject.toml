[project]
name = "youdaofanyi"
version = "0.1.0"
description = "FastAPI wrappers for captured Youdao translation requests."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
